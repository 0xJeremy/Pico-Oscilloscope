const minFrequency = 1;
const maxFrequency = 10000;
const defaultFrequency = 3000;

const minOffset = -3000;
const maxOffset = 3000;
const defaultOffset = 0;

export {
  minFrequency,
  maxFrequency,
  minOffset,
  maxOffset,
  defaultFrequency,
  defaultOffset,
};
